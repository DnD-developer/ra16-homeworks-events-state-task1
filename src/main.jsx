import React from "react"
import ReactDOM from "react-dom/client"
import Protfolio from "./components/portfolio-component/Portfolio"

ReactDOM.createRoot(document.getElementById("root")).render(
	<React.StrictMode>
		<Protfolio />
	</React.StrictMode>
)
